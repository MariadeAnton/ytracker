import React from 'react';

const Register = (props) => {
  const { firstname, lastname, email, password, password2, onChange } = props;
  return (
    <div>
      <h1>Register</h1>
    </div>
  );
}

export default Register;
